spring:
  application:
    name: pms-servuce
  cloud:
    nacos:
      config:
        server-addr:  127.0.0.1:8848
        namespace: 162cd20b-f4b8-4522-b3ce-dc46598a391c
        group: dev
        # properties 文件不用配置 但是yml配置文件后缀
        file-extension: yml
